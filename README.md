### Instruções para instalar dependências e executar os testes

Para instalar as dependencias execute os seguintes comandos:
- `npm install --verbose --no-fund --no-audit`
- `npm install --verbose --no-fund --no-audit sequelize sqlite3`

Para executar os testes execute o seguinte comando:
- `npm test`
